<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- BaseToolbar Title text appearance. -->
    <style name="TextAppearance.Widget.BaseToolbar.Title" parent="TextAppearance.Material3.Title.Large">
        <item name="android:textSize">14sp</item>
    </style>

    <!-- BaseToolbar Subtitle text appearance. -->
    <style name="TextAppearance.Widget.BaseToolbar.Subtitle" parent="TextAppearance.Material3.Title.Small">
        <item name="android:textSize">12sp</item>
    </style>


    <!-- ViewDivider style. -->
    <style name="ViewDivider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:layout_marginTop">@dimen/activity_vertical_margin</item>
        <item name="android:layout_marginBottom">@dimen/activity_vertical_margin</item>
        <item name="android:background">?android:attr/listDivider</item>
    </style>

</resources>
